# のぼり見積もり・注文ページ TODO

このファイルで要件と進捗を管理していく想定です。  
`[x]` が完了、`[ ]` が未対応です。

---

## 全体コンセプト

- [x] デザイン1種に対して1会計・1品番に制限する（カートの仕様）

---

## 画面構成・見出し

- [x] 大きな見出しを商品ごとに変更できるようにする（将来の管理画面から編集可能に）
  - 例：商品ごとに「〇〇専門 のぼり制作」などを差し替え

---

## 入力ステップ（①〜⑨）

- [x] ① サイズ選択（`SizeSelector`）
- [x] ② 生地選択（`FabricSelector`）
- [x] ③ 棒通しの仕立て（棒袋／チチなどを「仕立て」の観点で分かりやすく表示）
- [ ] ④ 縫製方法（縫製方法専用の選択UIを用意するかどうか整理） ※棒通し以外の縫製が増えた段階で検討
- [x] ⑤ 印刷枚数（数量入力＋デザインごとの枚数管理）
- [x] ⑥ データ・デザイン選択（完全データ入稿／デザイン依頼の切り替え）
- [x] ⑦ オプション情報（データ名・案件名の記入欄）
- [x] ⑧ 器具・付属品（`AccessoriesSelector`）
- [x] ⑨ 出荷日（具体的な出荷日を選択 or 表示するUIの検討）

---

## 納期まわり

- [x] 通常納期／お急ぎ納期の概ねの日数表示（例：通常7〜10営業日、特急3〜5営業日）
- [x] お急ぎ出荷希望のチェック（`ScheduleSelector` で rushSchedule を制御）
- [ ] 実カレンダーを使った「出荷日」もしくは「お届け希望日」の日付選択

---

## 付属品

- [x] 器具・付属品を任意の個数で選択できるUI（オプション価格での個数選択）
- [x] 付属品金額を見積もり金額の内訳に明示的に反映する
- [ ] 必要に応じて「Shopify 登録商品としての付属品一覧」を表示する導線

---

## デザインデータまわり

- [x] デザインデータで ZIP ファイルを入稿できるようにする（フロント側の受付）
- [x] 完全データ入稿のところに説明ページへの URL を設置（`/data-guide` へのリンク）
- [x] 完全データ入稿のところにテンプレートを最大 10 個までダウンロード表示
  - [x] テンプレート定義をコード内定数から外だしする（将来 Firestore などで管理）
  - [ ] 実ファイル置き場（ストレージ）の決定とパス設計

---

## レイアウト・画像

- [x] 仕様（サイズ）の上にバナーを設置し、3種スライドさせる（`BannerSlider`）
- [x] バナー画像を外部ストレージ＋設定データから差し替え可能にする
- [x] サイズに対応する画像を最大 3 種まで登録できるようにする
- [x] 生地に対応する画像を最大 3 種まで登録できるようにする
- [x] オプションに対応する画像を最大 3 種まで登録できるようにする
- [x] 画像 URL を Firestore 等の設定データから取得するように変更

---

## 数量・見積もり UI

- [x] PC でも数量を変更できる UI（`QuantityInput` ＋ `QuantityInputField`）
- [x] 右側お見積りの「サイズ・生地・オプション・数量」の右に「変更」ボタンを表示
  - [x] 「変更」を押したらそれぞれのブロック（サイズ／生地／オプション／数量）へスクロール
- [ ] 数量・オプションなどの変更が Shopify 側のカート商品情報（line item properties 等）に正しく反映されることの確認

---

## その他 UI・レイアウト

- [x] サイトの上段と下段は Shopify の TOP に合わせる（テーマのヘッダー／フッター利用）
- [ ] レイアウトイメージを「幕メーカー」を参考に微調整（余白・フォント・色など）

---

## お客様の声

- [x] ページ最下部に「お客様の声」セクションを設置
- [x] 固定テキストのサンプル文言に差し替え（現状）
- [ ] 必要に応じて Shopify のレビュー機能や外部レビュー連携に差し替え可能にする検討

