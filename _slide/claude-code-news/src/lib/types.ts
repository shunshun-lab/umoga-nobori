export interface DigestFrontmatter {
  date: string;
  title: string;
  itemCount: number;
}

export interface Digest {
  date: string;
  title: string;
  itemCount: number;
  content: string;
  html: string;
}
